name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  security-events: write

jobs:
  python-ci:
    uses: Sagargupta16/shared-workflows/.github/workflows/python-ci.yml@main

  security:
    uses: Sagargupta16/shared-workflows/.github/workflows/security-scan.yml@main
